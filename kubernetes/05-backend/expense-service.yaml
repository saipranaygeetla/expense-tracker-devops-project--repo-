apiVersion: v1
kind: Service

metadata:
  name: expense-service
  namespace: expense-dev

spec:

  selector:
    app: expense-app

  ports:

  - name: http
    port: 8080

    targetPort: 8080

    nodePort: 30080

  type: NodePort