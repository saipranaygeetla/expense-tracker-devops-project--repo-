apiVersion: rbac.authorization.k8s.io/v1
kind: Role

metadata:
  name: expense-reader
  namespace: expense-dev

rules:

- apiGroups: [""]

  resources:

  - pods

  verbs:

  - get

  - list

  - watch